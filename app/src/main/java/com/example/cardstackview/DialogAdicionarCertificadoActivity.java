package com.example.cardstackview;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class DialogAdicionarCertificadoActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Apenas um layout vazio, ou nem setar um layout.
        // Isso mantém a classe válida mas não usada.
        finish(); // Fecha imediatamente para garantir que não afete nada.
    }
}
